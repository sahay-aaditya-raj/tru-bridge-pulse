'use client';

// Public version of the donor search that removes auth gate and credentials requirement
import DonorSearchPublic from './publicView';

export default function DonorSearchPublicPage() {
  return <DonorSearchPublic />;
}
